<Window x:Class="PhaethonsNomikon.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PhaethonsNomikon"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <DockPanel>
        <StatusBar DockPanel.Dock="Bottom">
            <StatusBarItem>
                <ToggleButton x:Name="ToggleLogView" Content="Show Logs" IsChecked="True"
                              Checked="ToggleLogView_Checked" Unchecked="ToggleLogView_Unchecked"/>
            </StatusBarItem>

            <StatusBarItem>
                <TextBlock Text="Ready" />
            </StatusBarItem>
            <StatusBarItem>
                <TextBlock Text="User: Maksym" />
            </StatusBarItem>
        </StatusBar>
        <Grid x:Name="LogContainer">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" MinHeight="30"/> <!-- Log Controls -->
                <RowDefinition Height="5"/> <!-- Splitter -->
                <RowDefinition Height="*" MinHeight="30"/> <!-- Log View -->
            </Grid.RowDefinitions>

            <!-- Main Content -->
            <Grid Grid.Row="0" Background="Pink">
                <TextBlock Text="Main Content Area" VerticalAlignment="Center" HorizontalAlignment="Center" />
            </Grid>
            <GridSplitter Grid.Row="1" Height="5" HorizontalAlignment="Stretch" Background="Gray"/>
            <StackPanel Grid.Row="2" x:Name="LogViewBox" Background="Orange">
                <TextBlock Text="Main Content Area" VerticalAlignment="Center" HorizontalAlignment="Center" />
                <TextBox x:Name="LogView" FontFamily="Consolas" FontSize="12"
                         IsReadOnly="True" TextWrapping="Wrap" Text="Logs will appear here..."/>
            </StackPanel>
        </Grid>
    </DockPanel>
</Window>
