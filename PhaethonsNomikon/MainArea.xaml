<local:MyUserControl x:Class="PhaethonsNomikon.MainArea"
                     xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                     xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                     xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                     xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                     xmlns:local="clr-namespace:PhaethonsNomikon"
                     mc:Ignorable="d"
                     d:DesignHeight="300" d:DesignWidth="300">
    
    <Grid Background="Pink">
        <Grid.RowDefinitions>
            <RowDefinition Height="20"/> <!-- Log Controls -->
            <RowDefinition Height="*"/> <!-- Log View -->
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Main Content Area" VerticalAlignment="Center" HorizontalAlignment="Center" />
        <TabControl
            x:Name="MyTabControl"
            Grid.Row="1"
            ItemsSource="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type local:MainArea}}, Path=Tabs}"
            SelectedIndex="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type local:MainArea}}, Path=SelectedTabIndex, Mode=TwoWay}"
            VirtualizingStackPanel.IsVirtualizing="False">
            <TabControl.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Header}" />
                </DataTemplate>
            </TabControl.ItemTemplate>
            <TabControl.ContentTemplate>
                <DataTemplate>
                    <local:BrowserTab Model="{Binding}"/>
                </DataTemplate>
            </TabControl.ContentTemplate>
        </TabControl>
    </Grid>
</local:MyUserControl>
